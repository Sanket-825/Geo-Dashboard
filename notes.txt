1. Generated 100000 mock data 
    Command : In generate-mock-data.js, const TOTAL_ROWS = 100000;
              node generate-mock-data.js  

2. Initialize Node project : open the backend fodler in terminal 
    Command : npm init -y

3. Install Dependencies : 

    1. npm install express mongoose cors dotenv
    2. npm install -g nodemon

4. Create Backend Folder Structure : 

    backend/
        ├── config/
        ├── controllers/
        ├── models/
        ├── routes/
        ├── scripts/
        ├── .env
        ├── server.js
        └── package.json

5. Created Schema : 
    1.Inside models/Project.js Created Schema


6. Next is to SEED 100,000 RECORDS : 
    1. Install Faker : npm install @faker-js/faker
    2. Create Seeder File : backend/scripts/seedData.js
    3. Added a code in seedData.js
    4. Run Seeder : Inside backend terminal : node scripts/seedData.js
    5. Verify Data in Atlas : 
        Atlas Dashboard => Database → Browse Collections
        You should see: geo-dashboard => projects


7. Next we’ll build: Paginated Backend API

    1. We will NOT send all 100k rows to frontend.
        Instead 
       backend sends small chunks. 
    
    2. Goal : API like: GET /api/projects?page=1&limit=50

        Response: {
                    "projects": [...],
                    "total": 100000,
                    "page": 1,
                    "totalPages": 2000
                  }

    3. Create Controller : backend/controllers/projectController.js 
       Added a controller code 

    4. WHAT The code does : if page=2 and limit=50 then skip = 50, Meaning: skip first 50, fetch next 50

    5. Create Routes : backend/routes/projectRoutes.js 
       Added a Route Code         
    
    6. Connect Routes to Server : backend/server.js
        Add import: import projectRoutes from "./routes/projectRoutes.js"; 
        Then BELOW middleware:  app.use(express.json());
        Add : app.use("/api/projects", projectRoutes);
    
    7. Run Backend : npm run dev

    8. TEST API : http://localhost:5000/api/projects 

    9. Test Pagination : http://localhost:5000/api/projects?page=2&limit=10


8. Install axios : npm install axios


** CONNECT FRONTEND TO REAL API **
     
1. Create API Service : frontend/src/api/projectService.js

2. We dont need geoService.js now remove it later okay 

3. Added API Function to projectService.js

4. Updated Dashboard.jsx :

    Frontend page change
            ↓
    API request sent
            ↓
    Backend fetches only required rows
            ↓
    Frontend renders them


5. Updated DataTable.jsx 

6. Now run both terminal frontend and backend :

    PS F:\geo-dashboard\frontend> 
    PS F:\geo-dashboard\backend> => first connect the atlas and make sure your IP is accessible check in setting => network access 


7. Updated MapView.jsx 


** Virtualization **

1. Updated projectController.js 
    We have added => 
        search :  /api/projects?search=mumbai => MongoDB uses => $regex for partial matching.
        filtering : /api/projects?status=Active => Returns only active projects.
        sorting : /api/projects?sortBy=lastUpdated&order=asc
        dynamic query handling : 

2. TEST THESE IN BROWSER : 

    1. Search : http://localhost:5000/api/projects?search=Project 1
    2. Filter : http://localhost:5000/api/projects?status=Active
    3. Sorting : http://localhost:5000/api/projects?sortBy=lastUpdated&order=desc
    4. Combined : http://localhost:5000/api/projects?page=1&limit=10&status=Active&search=Project


3. NEXT STEP : we connect : frontend search + filters + sorting to this backend API.

4. Right now it only supports: page + limit

    We’ll upgrade it to support: search, status filter, sorting, dynamic params

5. NEXT STEP : Now we connect this in frontend state management.
    
    We’ll add : 
                search input
                filter dropdown
                backend-connected sorting
                debounced API calls

6. data logic belongs inside hooks => useGeoData.js 


** Test current performance ** 

    1. STEP 1 — INCREASE PAGE SIZE 
        useGeoData.js => const [limit] = useState(500);

        The MIT version restricts page size to 100 rows. To handle larger datasets efficiently, I relied on backend pagination and built-in virtualization rather than rendering excessively large pages.

    2. STEP 2 — OPEN CHROME TASK MANAGER

        You’ll see: Memory, CPU, GPU, JS memory for each tab.


    3. STEP 3 — NETWORK TEST 

        Go => Network tab => Change page => Observe => payload size => Should remain small








    
